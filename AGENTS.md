# CelesteReplay repo instructions

- 对 `CelesteReplay/` 下的代码、文档、脚本做出修改后，必须：
  1. 运行与改动相符的验证 / 测试；
  2. 重新打包交付 zip；
  3. 同步更新 `README.md`；
  4. 将最新 `CelesteAutoCut.zip` 更新到 Celeste 安装目录 `D:\Steam\steamapps\common\Celeste\Mods\CelesteAutoCut.zip`；
  5. 在 `CelesteReplay` 这个 git 仓库内执行一次 `git commit`。
- 若当前改动尚未完成测试、打包、README 同步、Celeste Mods 目录更新或 git commit，则不要把任务标记为完成。
